<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>openq__Schedule_WebService_Daily__c</fullName>
    <defaultValue>false</defaultValue>
    <deprecated>false</deprecated>
    <description>If this is checked, the scheduler will run daily ,else the scheduler will run hourly. By default this is unchecked, which means the scheduler will run hourly.</description>
    <externalId>false</externalId>
    <inlineHelpText>If this is checked, the scheduler will run daily ,else the scheduler will run hourly. By default this is unchecked, which means the scheduler will run hourly.</inlineHelpText>
    <label>Schedule_WebService_Daily</label>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
