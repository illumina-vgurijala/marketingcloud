<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>SVMX_PS_VS_Consumed_Stocked_Serial__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>The field indicates reference to Stock Serial. This will be used to find the proper Stock Serial under the consumed product stock. Either this field or &quot;Consumed Serial Number&quot; need to be populated in case if product is Serialized in order to get data.</description>
    <externalId>false</externalId>
    <inlineHelpText>The field indicates reference to Stock Serial. This will be used to find the proper Stock Serial under the consumed product stock. Either this field or &quot;Consumed Serial Number&quot; need to be populated in case if product is Serialized in order to get data.</inlineHelpText>
    <label>Consumed Stock Serial</label>
    <referenceTo>SVMXC__Product_Serial__c</referenceTo>
    <relationshipLabel>Work Details (Consumed Stock Serial)</relationshipLabel>
    <relationshipName>SVMX_PS_VS_Consumed_Stocked_Serial</relationshipName>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
